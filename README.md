# My Vim Setting

## Preconditions
this files put to ~/.vim

so.

```
cd ~/
git clone {this repo}

mv ~/{this repo} * ~/.vim
```


and

.vimrc put to home directory
```
ln -s ~/.vim/.vimrc ~/.vimrc
```

## using plugin manager
- vim-plug